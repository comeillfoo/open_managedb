package plants;

import entity.Organisation;

public class OrganizationBuilder implements Factory<Organisation> {
  @Override
  public <T> T create() {
  }
  @Override
  public <T> List<T> createFromFile(String pathname) {
  }
}